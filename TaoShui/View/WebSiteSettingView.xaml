<UserControl x:Class="TaoShui.View.WebSiteSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Margin="2 2 2 0"
             d:DesignWidth="900" d:DesignHeight="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0" ItemsSource="{Binding WebSiteSettings}" Margin="0 0 0 3">
            <DataGrid.Columns>
                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="网址" Binding="{Binding Url}" Width="*" />
                <DataGridTextColumn Header="用户名" Binding="{Binding LoginName}" Width="*" />
                <DataGridTextColumn Header="密码" Binding="{Binding Password}" Width="*" />
                <DataGridTextColumn Header="验证码长度" Binding="{Binding CaptchaLength}" Width="*" />
                <DataGridTextColumn Header="登录超时时间" Binding="{Binding LoginTimeOut}" Width="*" />
                <DataGridTextColumn Header="抓取数据时间间隔" Binding="{Binding GrabDataInterval}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
        <GridSplitter Grid.Row="0" Height="3" HorizontalAlignment="Stretch" VerticalAlignment="Bottom"></GridSplitter>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <DataGrid Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Margin="0 0 3 0"></DataGrid>
            <GridSplitter Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Width="3" HorizontalAlignment="Right" VerticalAlignment="Stretch"></GridSplitter>
            <DataGrid Grid.Row="0" Grid.Column="1" Margin="0 0 0 3"></DataGrid>
            <GridSplitter Grid.Row="0" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Height="3"></GridSplitter>
            <DataGrid Grid.Row="1" Grid.Column="1"></DataGrid>
            <DataGrid Grid.Row="0" Grid.Column="2" Grid.RowSpan="2" Margin="3 0 0 0"></DataGrid>
            <GridSplitter Grid.Row="0" Grid.Column="2" Grid.RowSpan="2" Width="3" HorizontalAlignment="Left" VerticalAlignment="Stretch"></GridSplitter>
        </Grid>
    </Grid>
</UserControl>